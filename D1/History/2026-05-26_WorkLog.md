# 2026-05-26 작업 로그

## 작업 목표
- `ClientTravel` (Town ↔ Dungeon) 시 `PlayerState` 데이터가 리셋되는 버그 해결
- Primary Attribute (Strength/Int 등) 및 `AttributePoints`가 맵 이동 후에도 유지되도록 구현

## 문제 상황
1. **PIE `ClientTravel`로 Dungeon 이동 시:**
   - 새 `PlayerState` 객체 생성 (`CONSTRUCTOR` 로그 확인)
   - `AttributePoints`가 5→0 또는 5→5로 돌아감 (투자한 포인트 반영 안 됨)
   - `bAbilitySystemInitialized`가 `FALSE`로 초기화 → `InitializeDefaultAttributes()`가 다시 실행됨
   - Primary Attribute(Strength 등)가 기본값으로 리셋됨
2. **원인:** PIE/Standalone에서는 `ClientTravel`이 기존 PlayerState를 유지하지 않고 **새로 생성**함
3. **초기 접근법:** `bAbilitySystemInitialized`를 `Replicated`로 설정했으나, PIE에서는 의미 없음 (새 객체 생성)

## 해결 방안 (GameInstance 임시 저장 패턴)
### 1. `UD1GameInstance` 신규 생성 (`Game/D1GameInstance.h/.cpp`)
- `SavePlayerStateData(bInit, AttrPts, Level, XP, Str, Int, Dex, Luc)`: Travel 직전 데이터 저장
- `RestorePlayerStateData(...)`: Travel 후 데이터 복원
- `ClearSavedData()`: 복원 후 클리어
- `HasSavedData()`: 저장 여부 확인

### 2. `AD1PlayerController` 수정 (`Player/D1PlayerController.h/.cpp`)
- `PreClientTravel(const FString& PendingURL, ETravelType TravelType, bool bIsSeamlessTravel)` 오버라이드 추가
  - Engine이 `ClientTravel` 직전에 **자동으로** 호출하는 함수 (BP_DungeonPortal이 `ClientTravel` 직접 호출해도 작동)
  - `PlayerState` + `AttributeSet` 데이터를 `GameInstance`에 저장
- `TravelToMap`에서는 별도 저장 로직 제거 (PreClientTravel이 처리)

### 3. `AD1PlayerState` 수정 (`Player/D1PlayerState.h/.cpp`)
- `RestoreTravelDataIfNeeded()` 함수 추가
  - `PossessedBy`에서 호출되도록 설계 (초기화보다 먼저)
  - `bAbilitySystemInitialized` 복원 → `InitializeDefaultAttributes()` 스킵 여부 결정
  - `AttributePoints`, `Level`, `XP` 복원
  - `Primary Attribute` (Strength/Int/Dex/Luc) 직접 `Set` 복원
  - `OnRep_*` 호출로 UI/Delegate 갱신

### 4. `AD1Hero` 수정 (`Characters/D1Hero.cpp`)
- `PossessedBy`에서 `InitAbilityActorInfo()` 직후 `D1PS->RestoreTravelDataIfNeeded()` 호출
- **복원 성공 시 (`bInit=TRUE`):**
  - `InitializeDefaultAttributes()` **스킵** (Primary GE 덮어쓰기 방지)
  - 대신 `Secondary GE + Vital GE`만 **재적용** (`ApplyEffectToSelf`)
  - 새 `AttributeSet`은 Secondary/Vital GE가 필요함
- **복원 실패 시 (최초 접속):**
  - `InitializeDefaultAttributes()` + `AddCharacterAbilities()` 정상 실행

### 5. `AD1GameModeBase` 수정 (`Game/D1GameModeBase.h/.cpp`)
- `PostLogin` 오버라이드 추가 → Travel 후 PlayerState 상태 디버그 로그

### 6. `DefaultEngine.ini` 수정
- `GameInstanceClass=/Script/D1.D1GameInstance` 추가
- **주의:** Project Settings → Maps & Modes → Game Instance Class에서도 `D1GameInstance` 선택 필요

## 테스트 결과
- **Town → Dungeon 이동:**
  - `PreClientTravel`: `bInit=TRUE, AttrPts=0, Str=11.0, Int=5.0...` 저장 확인
  - `PossessedBy`: `RestoreTravelDataIfNeeded` → `bInit=TRUE` 복원 → `InitializeDefaultAttributes` 스킵
  - `BeginPlay`: 이미 복원 완료된 상태
  - **결과:** AttributePoints=0, Strength=11.0 정상 유지 확인 ✅

## 변경 파일 목록
| 파일 | 변경 내용 |
|-----|----------|
| `Game/D1GameInstance.h` | 신규 생성 |
| `Game/D1GameInstance.cpp` | 신규 생성 |
| `Player/D1PlayerController.h` | `PreClientTravel` 선언 추가 |
| `Player/D1PlayerController.cpp` | `PreClientTravel` 구현, `D1AttributeSet` include |
| `Player/D1PlayerState.h` | `RestoreTravelDataIfNeeded()` 선언, `UD1GameInstance` forward decl |
| `Player/D1PlayerState.cpp` | `RestoreTravelDataIfNeeded()` 구현, `BeginPlay` 간소화 |
| `Characters/D1Hero.cpp` | `PossessedBy`에 복원 호출, Secondary+Vital 재적용 로직 |
| `Game/D1GameModeBase.h` | `PostLogin` 선언, `AD1PlayerState` forward decl |
| `Game/D1GameModeBase.cpp` | `PostLogin` 구현 |
| `Game/D1GameModeDungeon.h/.cpp` | **신규** — `OnBossDefeated()` |
| `UI/WidgetController/D1DungeonResultWidgetController.h/.cpp` | **신규** — 획득 아이템 관리 WC |
| `UI/HUD/D1HUD.h/.cpp` | `GetDungeonResultWidgetController()` 추가 |
| `AbilitySystem/D1AbilitySystemLibrary.h/.cpp` | `GetDungeonResultWidgetController()` 추가 |
| `Characters/D1Enemy.h/.cpp` | `bIsBoss`, `HasAuthority()` 가드 |
| `Config/DefaultEngine.ini` | `GameInstanceClass` 설정 |

## 변경 파일 목록 (2026-05-26 추가)
| 파일 | 변경 내용 |
|-----|----------|
| `D1GameInstance.h/.cpp` | 저장 항목에서 `bAbilitySystemInitialized` 제거 |
| `D1PlayerState.h/.cpp` | `bAbilitiesAllowed` Replicated 추가, `RestoreTravelDataIfNeeded`에서 `bInit` 복원 제거 |
| `D1PlayerController.h/.cpp` | `CanUseAbilities()`, `ClientShowDungeonResult()`, 디버그 로그 추가 |
| `D1Hero.cpp` | `PossessedBy` 순서 변경 (InitAttributes → Restore → AddAbilities) |

## 추가 작업 (2026-05-26 추가)

### 1. 던전 결과 위젯 시스템 (WidgetController 패턴 적용)
- **`D1DungeonResultWidgetController` 신규 생성 (`UI/WidgetController/`):**
  - `D1WidgetController` 상속
  - `SetAcquiredItems(const TArray<FText>&)` + `OnAcquiredItemsChanged` Delegate
- **`D1GameModeDungeon::OnBossDefeated()` (서버 전용):**
  - 보스 사망 시 모든 `PlayerController` 순회
  - `ClientShowDungeonResult(LootItems)` Client RPC 호출 (획득 아이템 데이터 포함)
- **`D1PlayerController::ClientShowDungeonResult_Implementation()` (클라이언트):**
  - `GetDungeonResultWidgetController()` → WC 생성
  - `CreateWidget` → `SetWidgetController` → `AddToViewport`
  - **바인딩 완료 후** `WC->SetAcquiredItems(AcquiredItems)` 호출 → Delegate 발송 → Blueprint 바인딩된 이벤트 실행
- **데이터 타이밍 이슈 해결:** RPC에 `TArray<FText>` 포함 → 위젯 생성(바인딩) → 데이터 세팅 순서로 변경

### 2. 보스 사망 감지
- **`D1Enemy::Die()` 수정:**
  - `HasAuthority()` 가드 추가 (AIController/SetLifeSpan는 서버 전용)
  - `bIsBoss` bool 추가 (EditAnywhere, BlueprintReadOnly)
  - `bIsBoss == true`일 때 `GameModeDungeon->OnBossDefeated()` 호출

### 3. 마을에서 Ability 사용 불가
- **`D1GameModeBase::AreAbilitiesAllowed()`** 가상 함수 추가 (기본 true)
- **`D1GameModeTown`**: `AreAbilitiesAllowed() = false` override
- **`D1PlayerState`**: `bAbilitiesAllowed` (Replicated) 추가 → GameMode::PostLogin에서 GameMode 규칙 동기화
- **`D1PlayerController::CanUseAbilities()`**: PlayerState의 `bAbilitiesAllowed` 반환 (클라이언트에서도 동작)
- **`AbilityInputTagPressed/Released/Held`**: `!CanUseAbilities()`이면 무시 (RMB 이동 제외)

### 4. Critical Bug Fix: ClientTravel 후 Ability 재등록 버그
- **문제:** Dungeon 이동 후 좌클릭 기본공격(Attack Ability) 작동 안 함
- **원인:** GameInstance에 `bAbilitySystemInitialized`를 저장/복원 → `PossessedBy`에서 `AddCharacterAbilities()`가 스킵됨 → 새 Pawn의 ASC에 Ability Spec이 없음
- **해결:**
  - `D1GameInstance`: 저장 항목에서 `bAbilitySystemInitialized` 제거
  - `D1PlayerState::RestoreTravelDataIfNeeded()`: `bInit` 복원 제거
  - `D1Hero::PossessedBy` 순서 변경:
    1. `InitializeDefaultAttributes()` (Primary 기본값 설정)
    2. `RestoreTravelDataIfNeeded()` (GameInstance 값으로 Primary Override)
    3. `AddCharacterAbilities()` (★항상 실행 — ASC 내부에서 중복 Spec 자동 스킵)
- **결과:** 맵 이동 후에도 기본공격, ChargeDash, Focus 등 모든 Ability 정상 작동

## 다음 작업
- [ ] `WBP_DungeonResult` Blueprint 생성 (Event Construct에서 HUD 경로로 WC 바인딩, 마을 귀환 버튼)
- [ ] `BP_D1PlayerController`의 Dungeon Result Widget Class 할당
- [ ] Dungeon 레벨에 보스 몬스터 배치 + bIsBoss=true 설정
- [ ] 던전 → 마을 귀환 포탈/버튼 구현
- [ ] 몬스터 AI Behavior Tree 튜닝 (추적, 공격)
- [ ] 중요: 현재 GameInstance 임시 저장은 **세션 내** 유효. Phase 3 웹서버 연동 시 영속화 필요

## 추가 작업 (2026-05-26 마무리)

### 5. `WBP_DungeonResult` Blueprint 생성 및 연결
- **문제:** Boss 처치 후 "보스 처치됨!" 로그는 뜨지만 UI 안 뜸
- **원인:** `BP_D1PlayerController`의 `Dungeon Result Widget Class`가 `None`이었음
- **해결:** `WBP_DungeonResult` Blueprint 생성 (`UD1UserWidget` 상속) → `BP_D1PlayerController`에 할당
  - `Event Construct`에서 `GetHUD` → `GetDungeonResultWidgetController` → `Bind to OnAcquiredItemsChanged` → `Update Item List`
  - "Return to Town" 버튼 `OnClicked` → `D1PlayerController::TravelToMap("Town")` 호출
- **결과:** 보스 처치 후 던전 결과 UI 정상 표시, 마을 귀환 버튼 작동 ✅

### 6. 레벨업 이펙트 중복 버그 수정 (맵 이동 시)
- **문제:** Town → Dungeon 이동 시 이미 Lv.1인 상태에서 레벨업 사운드/이펙트가 다시 재생됨
- **원인:** `D1PlayerState::RestoreTravelDataIfNeeded()`에서 값 복원 후 **무조건** `OnRep_Level()`, `OnRep_XP()`, `OnRep_AttributePoints()` 호출 → Blueprint 위젯/Overlay WC에서 "레벨 변경"으로 인식
- **해결:**
  - 복원 전 현재 값 저장 (`OldAttributePointsBeforeRestore`, `OldLevelBeforeRestore`, `OldXPBeforeRestore`)
  - 복원 후 **실제로 값이 달라졌을 때만** `OnRep_*` 호출
  - 수정 파일: `D1PlayerState.cpp`
- **결과:** 동일한 값(예: Level 1→1) 복원 시 `OnRep` 스킵 → 레벨업 이펙트 중복 재생 방지 ✅

## 참고 사항
- `PreClientTravel`은 Engine의 `APlayerController::ClientTravel` 직전에 항상 호출됨
- `GameInstance`는 PIE에서도 맵 전환 시 **살아남음** (UWorld와 별개 생명주기)
- `Cast<UD1AttributeSet>` 필요: `GetAttributeSet()`이 `UAttributeSet*`를 반환함
- Secondary Init GE는 **Instant Duration** (장비/버프 Add Modifier 충돌 방지)
- `CreateWidget`은 Blueprint `Event Construct`를 **동기적**으로 실행 → 바인딩 후 `SetAcquiredItems` 호출이 안전
