# 작업 로그: 2026-05-04

## 작업 내용

### 1. D1PlayerState에 InventoryComponent Getter 추가
- **파일:** `Source/D1/Player/D1PlayerState.h`
- **내용:** `UFUNCTION(BlueprintPure) UD1InventoryComponent* GetInventoryComponent() const` 추가
  - WidgetController 및 Blueprint에서 안전하게 인벤토리 컴포넌트에 접근 가능하도록 함

### 2. D1InventoryWidgetController 로직 구현
- **파일:** `Source/D1/UI/WidgetController/D1InventoryWidgetController.h`, `.cpp`
- **내용:**
  - `BroadcastInitialValues()`: 위젯 생성 시 현재 인벤토리 상태를 `OnInventoryUpdated` 델리게이트로 브로드캐스트
  - `BindCallbacksToDependencies()`: `InventoryComponent->OnInventoryChanged`에 `OnInventoryChangedCallback` 동적 바인딩
  - `GetInventorySlots()`: 현재 인벤토리 슬롯 배열 반환 (Blueprint Pure)
  - `GetItemData(WorldContext, ItemID)`: `UD1AbilitySystemLibrary`를 통해 아이템 메타데이터 룩업 (Blueprint Pure)
  - `UseItem(SlotIndex)`: `ServerUseItem` RPC 호출
  - `MoveItem(From, To)`: `ServerMoveItem` RPC 호출
  - `DiscardItem(SlotIndex)`: `ServerDiscardItem` RPC 호출
  - `OnInventoryUpdated` 델리게이트: `TArray<FD1InventoryItem>`를 파라미터로 하는 BlueprintAssignable 델리게이트

## 비고
- 현재 인벤토리 UI (`WBP_Inventory`, `WBP_InventorySlot`)는 Blueprint에서 구현 예정
- `UseItemInternal`의 실제 GE 적용 로직 및 장비 장착/탈착은 아직 미구현 (Phase 1 후반 예정)
- `D1InventoryWidgetController`를 통해 UI는 이미 서버-클리언트 동기화 및 RPC 호출이 가능한 상태
