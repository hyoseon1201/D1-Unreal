# 작업 로그: 2026-05-12

## 작업 내용

### 1. 장비 장착/탈착 UI 및 기능 완성 (인벤토리 Phase 1 마무리)
- **파일:** `WBP_Inventory`, `WBP_EquipmentSlot` (Blueprint), `WBP_ItemDetail`
- **구현:**
  - `WBP_Inventory` 내 장착 슬롯 영역 추가 (Weapon, Helmet, Armor, Gloves, Boots, Necklace, Ring — 총 7개)
  - `WBP_EquipmentSlot` 제작 및 `InventoryWidgetController` 연동
  - `OnEquippedItemsUpdated` 델리게이트 바인딩 → 장착 상태 변경 시 실시간 아이콘 갱신
  - 장착 슬롯 클릭 → `WBP_ItemDetail` 표시 (Unequip 버튼만 노출)
  - Unequip 버튼 클릭 → `ServerUnequipItem(EEquipmentSlot)` RPC → GE 제거 → 인벤토리 첫 빈 슬롯 반환
  - `For Each Loop` + `Break FD1EquippedItem`로 `EquippedItems` 배열 순회 및 각 슬롯 매핑
  - `Switch on EEquipmentSlot`으로 Weapon~Ring 슬롯 위젯 분기 (Branch 7중첩 제거)
- **C++ 백엔드 추가:**
  - `D1InventoryWidgetController`: `IsSlotEquipped(EEquipmentSlot)` 추가
  - `D1InventoryWidgetController`: `GetEquippedItemID(EEquipmentSlot)` 추가
  - `D1InventoryComponent`: `EquipItemInternal` 로그 강화 (장착 상태 순회 출력)

### 2. 멀티플레이어 테스트
- **Net Mode:** Play as Client (Dedicated Server 시뮬레이션)
- **테스트:** 2인 동시 접속 (Client A, Client B)
- **결과:**
  - 인벤토리 열기/닫기 정상 동기화
  - 아이템 사용 (소비) 후 HP 변화 클라이언트간 동기화 확인
  - 장비 장착 → 인벤토리에서 사라짐, 장착 슬롯에 아이콘 표시됨
  - 장비 탈착 → 인벤토리에 다시 추가됨, 장착 슬롯 빈칸으로 변경
  - `OnEquippedItemsChanged` 델리게이트 Replication 정상

### 3. 설계 확정 및 기술 부채 기록
- **장비 버프 방향 확정:** Secondary Attribute 직접 수정 불가 (`Override` Init GE가 `Add`를 덮어씀)
  - 장비는 Primary Attribute (STR/DEX/INT) 수정 방식으로 기획
  - `GE_Equip_Sword_Iron` → `Strength +15` (MMC_AttackPower가 자동 재계산)
  - AttributeMenu에서 실시간 스탯 변화 확인 완료
- **WBP_SlotBase 리팩토링 필요성 인식:**
  - 현재 `WBP_SlotBase`에 인벤토리/스킬/장비 기능이 몰려 있음 (상속 남용)
  - Phase 1 이후 `Composition` 패턴으로 분리 필요 (별도 위젯 포함 구조)
  - `WBP_InventorySlot` / `WBP_SkillSlot` / `WBP_EquipmentSlot` 독립화 예정

## 비고
- 인벤토리 Phase 1 (언리얼 납부) **100% 완료**
- `CONTEXT.md` 작업 항목 6번 → ✅ 완료 상태로 변경
- 다음 단계: 어빌리티 추가 개발 (기본 공격, 회피/대시, 광역 스킬, 버프/힐 등 3~5개)
- 어빌리티 완료 후 Phase 2: 웹서버 연동 (로그인/매칭/영속 데이터 저장)
